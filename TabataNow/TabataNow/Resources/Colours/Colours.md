#  Theme Colours

```swift

    private let bgTop = Color(hex: 0x0B1720)
    private let bgBottom = Color(hex: 0x162733)
    private let panel = Color(hex: 0x1C2E3A)
    private let panelInner = Color(hex: 0x12212B)
    private let muted = Color.white.opacity(0.65)
    private let dim = Color.white.opacity(0.35)
    private let neon = Color(hex: 0xB9FF63)
```



